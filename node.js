// node Read Eval  Print Loop (REPL) for the Go programming language.
// it is a computer environment where user inputs are read and evaluated and then the results are returned to the user.

// node name              to initiate the  REPL, run `node repl` in your terminal from the directory containing this file.
// .help                  to get help on REPL commands
// .edit <file>           Edit the file in your preferred editor
// .exit                  Exit the REPL
// ctrl+c ctrl+c          Force a shutdown of the process

// NODE native tool box         https://nodejs.org/docs/latest/api/documentation.html
// node package Manager(npm)    https://www.npmjs.com/

// const fs = require("fs");
// fs.writeFile("message.txt", "Hello Node", (err) => {
//   if (err) throw err;
//   console.log("The file has been saved!");
// });

// npm init                     Create a new npm package(package.json), or re-configure an existing one.
// package name  (name)         name of the package 
// version                      current version of the package  
// description (description)    long description of the package
// entry point (directory)      module name of the application
// test  command                test runner
// git repository               git remote url
// keywords                     list of keywords that describe
// author (shwetank)            author's name and email address
// license (Isc)                license type
// is this ok (y)               to ensure

// npm install [package]        Install a package(npm module)
// npm i () () ()               install all of them and add dependencies in our package.json

// var generateName = require("sillyname");
// var sillyName = generateName();
// console.log(`My name is ${sillyName}.`);

//if ("type" : "module",) in package under  main field will be executed when you import it as a module.
// import generateName from "sillyName";
// var sillyName = generateName();
// console.log(`My name is ${sillyName}.`);

// after that go to the package in npm site and then copy the thing and paste it in your index.js and  save it .
// npm is used to use other people created packages or codes to create something far better

